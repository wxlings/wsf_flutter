import 'package:flutter/material.dart';

class ProtocolPage extends StatelessWidget {
  const ProtocolPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Protocol Page"),
    );
  }
}
